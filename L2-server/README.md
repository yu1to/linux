
---

# 📂 L2-server/README.md

```markdown
# L2: Web + DB サーバ構築

## ■ 実施内容

### Nginxインストール
```bash
sudo apt install nginx -y
systemctl status nginx

# L2: Web + DB サーバ構築

## ■ Webサーバ（Nginx）

### インストール
```bash
sudo apt update
sudo apt install nginx -y
